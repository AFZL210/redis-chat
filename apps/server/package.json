{
    "name": "server",
    "version": "1.0.0",
    "private": true,
    "devDependencies": {
        "tsc-watch": "^6.0.4"
    },
    "scripts": {
        "start": "node dist/index",
        "dev": "tsc-watch --onSuccess \"node dist/index.js\"",
        "build": "tsc -p ."
    },
    "dependencies": {
        "ioredis": "^5.3.2",
        "socket.io": "^4.7.4"
    }
}
